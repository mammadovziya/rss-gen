services:
  rss-generator:
    build: .
    container_name: rss-generator
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      PORT: "3000"
      HOST: 0.0.0.0
      DATA_DIR: /data
    volumes:
      - ./data:/data
    restart: unless-stopped
